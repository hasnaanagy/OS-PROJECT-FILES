apiVersion: v1
kind: Pod
metadata:
  name: my-web-app
  labels:
    app: hasnaa
     
spec:
  containers:
    - name: my-nginx-app
      image: hasnaanagy/nginx-test
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 80
       